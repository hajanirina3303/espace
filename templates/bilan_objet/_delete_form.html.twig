<form method="post" action="{{ path('bilan_objet_delete', {'id': bilan_objet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bilan_objet.id) }}">
    <button class="btn">Delete</button>
</form>
