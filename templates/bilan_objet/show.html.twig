{% extends 'base.html.twig' %}

{% block title %}BilanObjet{% endblock %}

{% block body %}
    <h1>BilanObjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bilan_objet.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bilan_objet_index') }}">back to list</a>

    <a href="{{ path('bilan_objet_edit', {'id': bilan_objet.id}) }}">edit</a>

    {{ include('bilan_objet/_delete_form.html.twig') }}
{% endblock %}
