<form method="post" action="{{ path('piece_delete', {'idpiece': piece.idpiece}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece.idpiece) }}">
    <button class="btn">Delete</button>
</form>
