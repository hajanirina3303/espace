{% extends 'base.html.twig' %}

{% block title %}Depense{% endblock %}

{% block body %}
    <h1>Depense</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('depense_index') }}">back to list</a>
     {% if is_granted('ROLE_EDITEUR') %}

    <a href="{{ path('depense_edit', {'iddepense': depense.iddepense}) }}">edit</a>

    {{ include('depense/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
