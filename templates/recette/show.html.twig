{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('recette_index') }}">Voir liste</a>

    <a href="{{ path('recette_edit', {'idrecette': recette.idrecette}) }}">Modifier</a>

    {{ include('recette/_delete_form.html.twig') }}
{% endblock %}
