<form method="post" action="{{ path('releve_delete', {'idreleve': releve.idreleve}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ releve.idreleve) }}">
    <button class="btn">Delete</button>
</form>
