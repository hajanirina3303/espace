<span class="bg-danger text-center offset-5 col-2">{{ erreur }}</span>
{{ form_start(form) }}
    {{ form_widget(form) }}
    </br>
     <button class="btn btn-primary float-center"  type="btn" >{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
