<form method="post" action="{{ path('don_objet_delete', {'id': don_objet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ don_objet.id) }}">
    <button class="btn">Delete</button>
</form>
