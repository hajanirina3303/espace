{% extends 'base.html.twig' %}

{% block title %}Approvisionnement{% endblock %}

{% block body %}
    <h1>Approvisionnement</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('approvisionnement_index') }}">back to list</a>

     {% if is_granted('ROLE_EDITEUR') %}

    <a href="{{ path('approvisionnement_edit', {'idapprovisionnement': approvisionnement.idapprovisionnement}) }}">edit</a>

    {{ include('approvisionnement/_delete_form.html.twig') }}

    {% endif %}
{% endblock %}
